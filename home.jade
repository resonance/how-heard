extends /layout.jade
block content

  .container
    .main
      .page-header
       h1 Marketing Sources
        .row
          .col-md-6
            if choices
              table.table.table-striped
                tbody
                  each selection in choices.selections
                    tr
                      td(data-choice="#{selection}") #{selection}
                      td
                        a(href="/") Delete

            div
              form(action='/#{shopName}/add' id="addselections" name='addselections', method="post")
                div
                  label(for="selection") Enter your selections
                  textarea.coupons-text(name="selection" rows="10")
                  button(type="submit" style="margin: 10px;") Add

script(src="//ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js")